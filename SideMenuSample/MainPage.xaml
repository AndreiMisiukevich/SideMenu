<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:side="clr-namespace:SideMenu;assembly=SideMenu"
             x:Class="SideMenuSample.MainPage">

    <side:SideMenuView x:Name="SideMenu">
        <!--MainView-->
        <ScrollView BackgroundColor="White">
            <StackLayout HeightRequest="2000">
                <StackLayout Orientation="Horizontal">
                    <Button Text="LEFT" Clicked="OnLeftButtonClicked"/>
                    <Button Text="RIGHT" Clicked="OnRightButtonClicked" HorizontalOptions="EndAndExpand"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>
        <!--LeftMenu-->
        <ScrollView side:SideMenuView.Place="LeftMenu"
                    BackgroundColor="Orange"
                    WidthRequest="250">
            <StackLayout HeightRequest="2000">
                <Button Text="RIGHT" Clicked="OnRightButtonClicked" />
            </StackLayout>
        </ScrollView>
        <!--RightMenu-->
        <ScrollView side:SideMenuView.Place="RightMenu"
                    BackgroundColor="Gold"
                    side:SideMenuView.MenuWidthPercentage="0.3">
            <StackLayout HeightRequest="2000">
                <Button Text="LEFT" Clicked="OnLeftButtonClicked" />
            </StackLayout>
        </ScrollView>
    </side:SideMenuView>
</ContentPage>